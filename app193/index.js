import { AppRegistry } from 'react-native';
import App from './src/App';

AppRegistry.registerComponent('app193', () => App);
AppRegistry.runApplication('app193', {
  rootTag: document.getElementById('root'),
});